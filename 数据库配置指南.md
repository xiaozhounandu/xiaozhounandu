# 数据库配置指南

## 🔧 配置问题修复

**问题**: Spring Boot启动失败，错误信息 "Failed to determine a suitable driver class"

**原因**: `application.yml` 配置文件使用了错误的根节点 `frontspring`，应该是 `spring`

**✅ 已修复**: 配置文件已更正为标准的Spring Boot配置格式

## 📋 数据库配置选项

### 选项1: 使用远程数据库 (默认配置)
当前 `application.yml` 已配置为连接远程数据库：
```yaml
spring:
  datasource:
    url: jdbc:mysql://42.193.124.96:3306/xiaozhounandu_v2
    username: root
    password: xiaozhounandu
```

### 选项2: 使用本地数据库
如果你有本地MySQL数据库，可以使用本地配置：

1. **创建本地数据库**:
```sql
CREATE DATABASE xiaozhounandu_v2 CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

2. **执行初始化脚本**:
```bash
mysql -u root -p xiaozhounandu_v2 < src/main/resources/sql/init-database.sql
```

3. **使用本地配置启动**:
```bash
java -jar target/xiaozhounandu-0.0.1-SNAPSHOT.jar --spring.profiles.active=local
```

或者在IDE中设置VM选项:
```
-Dspring.profiles.active=local
```

## 🚀 启动步骤

### 第一步: 验证数据库连接
访问测试接口检查数据库连接状态：
```
http://localhost:8080/api/test/db
```

预期返回：
```json
{
  "success": true,
  "message": "数据库连接正常",
  "adminUser": {
    "id": 1,
    "username": "admin",
    "role": "ADMIN"
  }
}
```

### 第二步: 启动后端服务
```bash
cd /Users/weizhijie/Desktop/java/xiaozhounandu
mvn spring-boot:run
```

或者使用IDE直接运行 `XiaozhounanduApplication.java`

### 第三步: 生成测试数据 (可选)
如果数据库中没有数据，执行测试数据生成脚本：
```bash
mysql -u root -p xiaozhounandu_v2 < src/main/resources/sql/test-data-generator.sql
```

### 第四步: 验证API接口
测试关键API接口：

1. **用户统计**:
```bash
curl -H "Authorization: Bearer YOUR_TOKEN" \
     http://localhost:8080/api/stats/summary
```

2. **看板数据**:
```bash
curl -H "Authorization: Bearer YOUR_TOKEN" \
     http://localhost:8080/api/stats/dashboard
```

## 📱 数据库连接配置说明

### application.yml (主配置)
- 使用远程数据库 `42.193.124.96:3306`
- 适用于生产环境或服务器环境

### application-local.yml (本地配置)
- 使用本地数据库 `localhost:3306`
- 适用于开发和调试环境

## 🔍 常见连接问题

### 1. 连接超时
- 检查网络连接
- 确认数据库服务器可访问
- 验证防火墙设置

### 2. 认证失败
- 检查用户名密码
- 确认数据库用户权限
- 验证数据库名称

### 3. 驱动问题
- 确认MySQL驱动依赖
- 检查驱动版本兼容性
- 验证JDBC URL格式

## 🎯 推荐配置

### 开发环境
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/xiaozhounandu_v2?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: [你的本地MySQL密码]
```

### 生产环境
```yaml
spring:
  datasource:
    url: jdbc:mysql://[数据库服务器]:3306/xiaozhounandu_v2?useSSL=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: [生产用户名]
    password: [生产密码]
```

## ✅ 验证成功标志

应用启动成功后，控制台应该显示：
```
Started XiaozhounanduApplication in X.XXX seconds
Tomcat started on port(s): 8080 (http)
```

并且可以访问：
- 测试接口: http://localhost:8080/api/test/db
- 登录接口: http://localhost:8080/api/auth/login

---

**配置修复完成**: ✅ 数据库连接配置问题已解决
**下一步**: 启动应用并验证API接口正常工作