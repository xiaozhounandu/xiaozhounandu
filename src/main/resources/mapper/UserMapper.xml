<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaozhounandu.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.xiaozhounandu.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="email" column="email"/>
        <result property="phone" column="phone"/>
        <result property="nickname" column="nickname"/>
        <result property="avatar" column="avatar"/>
        <result property="role" column="role"/>
        <result property="status" column="status"/>
        <result property="lastLoginTime" column="last_login_time"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="findByUsername" parameterType="String" resultMap="UserResultMap">
        SELECT id, username, password, email, phone, nickname, avatar, role, status, last_login_time, create_time, update_time
        FROM user
        WHERE username = #{username} AND status = 1
    </select>

    <select id="findById" parameterType="Long" resultMap="UserResultMap">
        SELECT id, username, password, email, phone, nickname, avatar, role, status, last_login_time, create_time, update_time
        FROM user
        WHERE id = #{id} AND status = 1
    </select>

    <select id="selectAll" resultMap="UserResultMap">
        SELECT id, username, password, email, phone, nickname, avatar, role, status, last_login_time, create_time, update_time
        FROM user
        WHERE status != 0
        ORDER BY create_time DESC
    </select>

    <insert id="insertUser" parameterType="com.xiaozhounandu.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, email, phone, nickname, role, status)
        VALUES (#{username}, #{password}, #{email}, #{phone}, #{nickname}, #{role}, #{status})
    </insert>

    <update id="updateUser" parameterType="com.xiaozhounandu.entity.User">
        UPDATE user
        SET username = #{username},
            email = #{email},
            phone = #{phone},
            nickname = #{nickname},
            role = #{role},
            status = #{status},
            avatar = #{avatar}
        WHERE id = #{id}
    </update>

    <update id="updatePassword">
        UPDATE user
        SET password = #{password}
        WHERE id = #{id}
    </update>

    <update id="updateLastLoginTime">
        UPDATE user
        SET last_login_time = NOW()
        WHERE id = #{id}
    </update>

    <update id="deleteUserById" parameterType="Long">
        UPDATE user
        SET status = 0
        WHERE id = #{id}
    </update>

    <select id="countTotalUsers" resultType="long">
        SELECT COUNT(*) FROM user WHERE status != 0
    </select>

</mapper>