spring:
  application:
    name: xiaozhounandu
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # 请根据实际情况修改数据库连接信息
    url: jdbc:mysql://42.193.124.96:3306/xiaozhounandu_v2?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: xiaozhounandu
  # Jackson配置
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
    default-property-inclusion: non_null
    serialization:
      write-dates-as-timestamps: false
    deserialization:
      fail-on-unknown-properties: false

# 开发环境日志配置
logging:
  level:
    com.xiaozhounandu: DEBUG
    org.springframework.web: DEBUG

server:
  port: 8080
  servlet:
    context-path: ""

mybatis:
  mapper-locations: classpath*:mapper/*.xml
  type-aliases-package: com.xiaozhounandu.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

