# 🚀 部署与测试检查清单

## 环境准备

### 前端环境检查
- [ ] Node.js 版本 ≥ 16.x
- [ ] npm 版本 ≥ 8.x
- [ ] 确认 package.json 依赖已安装
- [ ] 确认 vite.config.js 配置正确

### 后端环境检查
- [ ] Java 版本 ≥ 17 (Spring Boot 3.0)
- [ ] MySQL 8.0+ 已启动
- [ ] 后端端口 8080 可用
- [ ] 数据库已创建并初始化数据

---

## 启动步骤

### 1️⃣ 启动后端
```bash
# 进入后端项目目录
cd /path/to/spring-boot-backend

# 编译并运行
mvn clean package
java -jar target/customer-management-1.0.0.jar

# 或直接运行
mvn spring-boot:run

# 检查日志
# 应看到: "Tomcat started on port: 8080"
```

### 2️⃣ 启动前端
```bash
# 进入前端项目目录
cd /Users/weizhijie/Desktop/xiaozhounandu-main/v-xiaozhounandu/v-xiaozhounandu

# 安装依赖（已完成）
npm install

# 启动开发服务器
npm run dev

# 检查控制台输出
# 应看到: "Local: http://localhost:5173"
```

### 3️⃣ 验证服务
- [ ] 访问 http://localhost:5173 能打开登录页
- [ ] 后端 API 可访问 http://localhost:8080/api/auth/health (备用)
- [ ] 浏览器控制台无 CORS 错误
- [ ] Network 中 API 请求状态码为 200

---

## 功能测试清单

### 🔐 认证模块
- [ ] **注册测试**
  - 用户名 < 3位 → 提示错误
  - 密码 < 6位 → 提示错误
  - 密码不一致 → 提示错误
  - 正确注册 → 跳转登录页

- [ ] **登录测试**
  - 错误用户名/密码 → 提示失败
  - 正确登录 → 跳转首页
  - 已登录访问登录页 → 自动跳转首页

- [ ] **登出测试**
  - 点击登出 → 跳转登录页
  - localStorage 清空

### 📊 数据看板
- [ ] **统计数据显示**
  - 总客户数正确
  - 今日跟进数正确
  - 待跟进数正确

- [ ] **客户状态分布**
  - 进度条正确显示
  - 各状态数量正确

- [ ] **最近跟进列表**
  - 显示最近5条记录
  - 格式正确

### 👥 客户管理
- [ ] **查看客户列表**
  - 分页正常（每页10条）
  - 数据显示正确

- [ ] **搜索功能**
  - 按名称搜索 → 筛选正确
  - 按状态筛选 → 筛选正确
  - 按来源筛选 → 筛选正确

- [ ] **新增客户**
  - 必填项验证（名称、状态、来源）
  - 手机号格式验证
  - 邮箱格式验证
  - 创建成功 → 返回列表

- [ ] **编辑客户**
  - 加载详情正常
  - 修改保存成功

- [ ] **删除客户**
  - 二次确认弹窗
  - 删除成功 → 列表刷新

- [ ] **客户详情**
  - 基本信息正确显示
  - 跟进记录列表
  - 统计信息正确

- [ ] **客户转移**
  - 管理员/经理可见
  - 转移后负责人更新

### 📝 跟进记录
- [ ] **新增跟进**
  - 快速记录弹窗
  - 必填项验证
  - 添加成功 → 列表刷新

- [ ] **跟进列表**
  - 分页正常
  - 权限控制（普通用户只看自己的）

- [ ] **筛选功能**
  - 按类型筛选
  - 按时间范围筛选
  - 按负责人筛选（管理员）

- [ ] **编辑/删除**
  - 仅能操作自己的记录
  - 成功后刷新列表

### 📋 系统日志（仅管理员）
- [ ] **操作日志**
  - 记录所有关键操作
  - 筛选功能正常
  - 显示操作者、时间、IP

- [ ] **登录日志**
  - 记录登录/失败
  - 显示登录统计
  - 显示地理位置、浏览器

### 👤 个人中心
- [ ] **信息查看**
  - 显示当前用户信息
  - 角色徽章正确

- [ ] **修改资料**
  - 填写表单
  - 保存成功 → 即时更新

- [ ] **修改密码**
  - 当前密码错误 → 提示
  - 新密码 < 6位 → 提示
  - 两次密码不一致 → 提示
  - 成功修改 → 强制重新登录

### 🎫 权限测试
**管理员账号 (admin/admin123)**
- [ ] 可访问所有页面
- [ ] 可看到日志菜单
- [ ] 可执行所有操作

**经理账号 (manager/admin123)**
- [ ] 可访问除日志外所有页面
- [ ] 可转移客户
- [ ] 不可查看日志

**普通用户 (user/admin123)**
- [ ] 可访问基础页面
- [ ] 只能看到自己的数据
- [ ] 不可删除客户
- [ ] 不可转移客户
- [ ] 不可查看他人日志

---

## 页面链接测试

| 页面 | URL | 预期行为 |
|------|-----|----------|
| 登录 | `/login` | 未登录显示，登录后跳转 |
| 注册 | `/register` | 未登录显示 |
| 数据看板 | `/` | 需登录 |
| 客户列表 | `/customers` | 需登录 |
| 客户详情 | `/customers/detail/1` | 需登录，权限检查 |
| 客户表单 | `/customers/form` | 需登录 |
| 客户编辑 | `/customers/form/1` | 需登录 |
| 跟进记录 | `/followups` | 需登录 |
| 操作日志 | `/system/logs/operations` | 需管理员 |
| 登录日志 | `/system/logs/login` | 需管理员 |
| 个人中心 | `/user/profile` | 需登录 |
| 404页面 | `/不存在的路径` | 显示404 |

---

## 🔍 测试数据准备

### 测试账号
```javascript
// 管理员
{ username: 'admin', password: 'admin123', role: 'ADMIN' }

// 销售经理
{ username: 'manager', password: 'admin123', role: 'MANAGER' }

// 普通用户
{ username: 'user', password: 'admin123', role: 'USER' }
```

### 测试客户数据
```
1. 张三 - 潜在客户 - 网络咨询
2. 李四 - 已联系 - 客户推荐
3. 王五 - 谈判中 - 广告投放
4. 赵六 - 已成交 - 线下活动
5. 孙七 - 已流失 - 网络咨询
```

---

## 🐛 常见问题排查

### 问题1: 页面空白/白屏
**可能原因:**
- 后端未启动
- 端口被占用
- 路径配置错误

**解决方案:**
```bash
# 检查后端
curl http://localhost:8080

# 检查前端
npm run dev -- --port 5173
```

### 问题2: 登录后跳转404
**可能原因:**
- 本地存储的用户数据格式错误

**解决方案:**
```javascript
// 浏览器控制台执行
localStorage.clear()
location.reload()
```

### 问题3: API请求404
**可能原因:**
- vite proxy 配置错误
- 后端接口路径不一致

**检查:**
- vite.config.js 中 proxy 目标地址
- 后端 @RequestMapping 前缀

### 问题4: CORS 跨域错误
**可能原因:**
- 后端未配置跨域

**后端解决方案 (Spring Boot):**
```java
@Configuration
public class CorsConfig implements WebMvcConfigurer {
    @Override
    public void addCorsMappings(CorsRegistry registry) {
        registry.addMapping("/api/**")
            .allowedOrigins("http://localhost:5173")
            .allowedMethods("*")
            .allowedHeaders("*")
            .allowCredentials(true);
    }
}
```

### 问题5: Token 失效/未获取
**可能原因:**
- 登录成功未正确存储
- 请求头未添加

**检查:**
```javascript
// 浏览器控制台
localStorage.getItem('token')
localStorage.getItem('user')
```

---

## ✅ 性能优化建议

### 前端优化
- [ ] 图片压缩（如使用）
- [ ] 组件懒加载（路由级别已完成）
- [ ] 减少 console.log 生产环境
- [ ] 开启 gzip 压缩 (vite build)

### 体验优化
- [ ] 添加加载骨架屏
- [ ] 使用 Message 代替 alert
- [ ] 添加面包屑导航
- [ ] 优化表格空状态显示

---

## 📱 移动端适配检查

- [ ] 登录页在手机上自适应
- [ ] 侧边栏可正常折叠
- [ ] 表单输入不被键盘遮挡
- [ ] 按钮大小适合手指点击

---

## 🎨 UI/UX 检查

- [ ] 颜色协调，无视觉冲突
- [ ] 字体大小适中
- [ ] 点击区域足够大
- [ ] 加载动画流畅
- [ ] 错误提示友好
- [ ] 操作反馈及时

---

## 🔐 安全检查

- [ ] 密码在传输中加密（已使用 HTTPS 配置）
- [ ] Token 存储在 localStorage（可改进为 httpOnly cookie）
- [ ] 敏感信息脱敏显示（手机号、邮箱）
- [ ] 二次确认删除操作
- [ ] XSS 防护（Vue 自动转义）
- [ ] 接口参数校验后端验证

---

## 📊 完整业务流程测试

### 完整场景示例
```
1. 新用户注册 → 登录 → 创建客户 → 添加跟进 → 查看看板
2. 管理员登录 → 查看日志 → 转移客户 → 检查权限
3. 经理登录 → 编辑客户 → 删除跟进 → 导出数据（预留）
4. 普通用户 → 只能操作自己的数据 → 无法查看他人信息
```

---

## 🚀 生产环境部署

### 前端打包
```bash
cd /Users/weizhijie/Desktop/xiaozhounandu-main/v-xiaozhounandu/v-xiaozhounandu
npm run build

# 产物在 dist/ 目录
# 可部署到 Nginx, Apache, 或 CDN
```

### Nginx 配置示例
```nginx
server {
    listen 80;
    server_name yourDomain.com;

    location / {
        root /var/www/html/dist;
        try_files $uri $uri/ /index.html;
        index index.html;
    }

    location /api/ {
        proxy_pass http://localhost:8080/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

---

## 测试状态记录

### 日期: ___ 2025-12-18 ___
**测试人员**: ____________________

| 模块 | 测试结果 | 问题描述 | 优先级 |
|------|----------|----------|--------|
| 认证 | ⬜ 未测 | - | - |
| 客户管理 | ⬜ 未测 | - | - |
| 跟进记录 | ⬜ 未测 | - | - |
| 数据看板 | ⬜ 未测 | - | - |
| 日志系统 | ⬜ 未测 | - | - |
| 个人中心 | ⬜ 未测 | - | - |
| 权限控制 | ⬜ 未测 | - | - |

**严重问题记录**: ____________________

**建议改进**: ____________________

**总体验收**: ⬜ 通过 ⬜ 不通过

---

**本清单由 AI 生成，开发者需根据实际情况逐项测试确认**
**建议打印此清单，逐项勾选，发现问题立即记录**